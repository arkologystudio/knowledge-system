# Gamma Memo

A nested fixture to prove the corpus walk recurses into subdirectories. The
bulk source should discover this file under nested/ and land it as its own
artefact with a distinct content hash from the top-level documents.
