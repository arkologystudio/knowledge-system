# Alpha Report

This is the first fixture document for the deterministic bulk-artefact
ingestion pipeline. It is markdown with a heading, so the extractor should
derive the title "Alpha Report".

The body carries enough prose that the recursive chunker produces at least one
chunk. Widget-co shipped the acme-seed integration this quarter and the
retrieval substrate indexed it without an authored page.
